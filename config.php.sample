<?php
/**
 * Copy to config.php. Used to seed the SQLite config on first load (when DB is empty).
 * After that, all changes are made in the admin panel and stored in data/config.sqlite.
 *
 * For secure admin access, set admin_secret before first run so you can open admin.php?key=<that_value>.
 * If both admin_secret and update_secret are empty, the admin panel is not accessible.
 */
return [
    'update_repo' => 'Darknetzz/php-qrcodegenerator',
    'update_ip_allowlist' => '',
    'update_allow_app_any_ip' => '1',
    'update_use_basic_auth' => '0',
    'update_require_login_always' => '0',
    'update_auth_user' => '',
    'update_auth_password' => '',
    'update_secret' => '',
    'admin_secret' => '',
];
